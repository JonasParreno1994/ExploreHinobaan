import InputError from '@/components/input-error';
import { SiteBrand } from '@/components/site-brand';
import { Head, Link, useForm } from '@inertiajs/react';
import { LogIn } from 'lucide-react';
import { FormEvent } from 'react';

export default function TouristLogin() { const form=useForm({email:'',password:'',remember:false}); const submit=(e:FormEvent)=>{e.preventDefault();form.post('/tourist/login')}; return <div className="grid min-h-screen place-items-center bg-[#FFFBF5] px-5 py-10"><Head title="Tourist Login"/><div className="w-full max-w-md rounded-3xl border border-orange-100 bg-white p-8 shadow-xl"><Link href="/"><SiteBrand subtitle="Tourist Account"/></Link><h1 className="mt-8 text-3xl font-extrabold">Welcome back</h1><p className="mt-2 text-slate-500">Manage your reservations and saved tourist information.</p><form onSubmit={submit} className="mt-7 grid gap-4"><label className="text-sm font-bold">Email<input type="email" value={form.data.email} onChange={e=>form.setData('email',e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-4 font-normal" required/></label><InputError message={form.errors.email}/><label className="text-sm font-bold">Password<input type="password" value={form.data.password} onChange={e=>form.setData('password',e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-slate-200 px-4 font-normal" required/></label><button disabled={form.processing} className="mt-2 inline-flex h-12 items-center justify-center gap-2 rounded-xl bg-[#F97316] font-bold text-white"><LogIn className="size-5"/>Login</button></form><p className="mt-6 text-center text-sm">New here? <Link href="/tourist/register" className="font-bold text-[#0F766E]">Create tourist account</Link></p></div></div> }
