import InputError from '@/components/input-error'; import { TouristLayout } from '@/components/tourist/tourist-layout'; import { Head, useForm } from '@inertiajs/react'; import { FormEvent } from 'react';
export default function Verification({verification,idTypes}:any){const form=useForm({id_type:'',id_front:null as File|null,id_back:null as File|null,selfie_with_id:null as File|null,privacy_consent:false});const submit=(e:FormEvent)=>{e.preventDefault();form.post('/tourist/verification',{forceFormData:true})};const locked=verification?.verification_status==='verified';return <TouristLayout><Head title="Identity Verification"/><div className="rounded-3xl border border-orange-100 bg-white p-7"><h1 className="text-3xl font-extrabold">Verify Your Identity</h1><p className="mt-3 max-w-2xl text-slate-500">To maintain trusted reservations, submit a government-issued ID and a selfie holding the same ID. Tourism enterprises only see your verification status—never these files.</p>{verification&&<div className="mt-5 rounded-xl bg-teal-50 p-4"><strong className="capitalize text-[#0F766E]">Status: {verification.verification_status.replaceAll('_',' ')}</strong>{verification.rejection_reason&&<p className="mt-2 text-red-700">Reason: {verification.rejection_reason}</p>}</div>}{!locked&&<form onSubmit={submit} className="mt-7 grid gap-5"><label className="font-bold">ID type<select value={form.data.id_type} onChange={e=>form.setData('id_type',e.target.value)} className="mt-2 h-12 w-full rounded-xl border px-4" required><option value="">Select accepted ID</option>{idTypes.map((type:string)=><option key={type}>{type}</option>)}</select><InputError message={form.errors.id_type}/></label>{[['Front ID image','id_front',true],['Back ID image (optional)','id_back',false],['Selfie holding the same ID','selfie_with_id',true]].map(([label,key,required]:any)=><label key={key} className="font-bold">{label}<input type="file" accept="image/jpeg,image/png" required={required} onChange={e=>form.setData(key,e.target.files?.[0]??null)} className="mt-2 block w-full rounded-xl border p-3 font-normal"/><InputError message={(form.errors as any)[key]}/></label>)}<div className="rounded-2xl bg-[#FFF3E6] p-5 text-sm text-slate-600"><strong className="text-slate-900">Privacy Notice</strong><p className="mt-2">Purpose: Tourist identity verification. Access: authorized platform administrators only. Enterprises receive only your verification status. Documents follow the portal’s approved retention policy.</p><label className="mt-4 flex gap-3 font-semibold"><input type="checkbox" checked={form.data.privacy_consent} onChange={e=>form.setData('privacy_consent',e.target.checked)}/>I have read and agree to the Privacy Notice.</label><InputError message={form.errors.privacy_consent}/></div><button disabled={form.processing} className="h-12 rounded-xl bg-[#F97316] font-bold text-white">Submit for Verification</button></form>}</div></TouristLayout>}
